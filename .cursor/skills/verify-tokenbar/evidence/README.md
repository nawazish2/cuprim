# Evidence directory

Verification runs write claim folders here:

```
.cursor/skills/verify-tokenbar/evidence/<claim-slug>/
```

Keep logs and screenshots. Do not commit large binaries unless the user asks; this path is for local/agent proof. Cleanup must not delete this tree.
